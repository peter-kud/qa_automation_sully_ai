{
  "name": "qa-automation-sully-ai",
  "version": "1.0.0",
  "description": "QA Automation Framework for Sully AI using Playwright",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:run": "./run-tests.sh",
    "allure:serve": "allure serve reports/allure-results",
    "allure:gen": "allure generate reports/allure-results --clean -o reports/allure-report",
    "clean": "rm -rf reports/* && mkdir -p reports/test-output reports/videos reports/screenshots reports/traces",
    "reports": "allure generate reports/allure-results --clean -o reports/allure-report && allure open reports/allure-report"
  },
  "keywords": ["playwright", "automation", "testing", "allure"],
  "author": "QA Team",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "allure-playwright": "^2.12.0",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
